[project]
name = "dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbt-core~=1.8.0",
    "dbt-postgres~=1.8.0",
    "dbt-spark[pyhive]>=1.10.1",
]
