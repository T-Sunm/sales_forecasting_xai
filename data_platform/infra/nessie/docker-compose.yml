version: '3.8'

services:
  nessie:
    image: ghcr.io/projectnessie/nessie:0.107.2-java
    container_name: nessie_container
    environment:
      # Nessie configuration
      - NESSIE_VERSION_STORE_TYPE=JDBC
      - NESSIE_VERSION_STORE_PERSIST_JDBC_DATASOURCE=postgresql
      
      # Datasource configuration (connect to existing postgres_container)
      - QUARKUS_DATASOURCE_POSTGRESQL_JDBC_URL=jdbc:postgresql://postgres_container:5432/nessie
      - QUARKUS_DATASOURCE_POSTGRESQL_USERNAME=postgres
      - QUARKUS_DATASOURCE_POSTGRESQL_PASSWORD=changeme # Match with your .env
      
      # Health and management
      - QUARKUS_HTTP_PORT=19120
    ports:
      - "19120:19120"
      - "9010:9000"
    networks:
      - data_platform_net
    restart: unless-stopped

networks:
  data_platform_net:
    external: true
    name: data_platform_net
